"use client";

import { useRef, useState } from "react";

export const Video_call = ()=> {
    const [pc,set_pc] = useState({});
   const local_video_ref = useRef(null);
   const remote_video_ref = useRef(null);
   const peers_ref = useRef(null);
    return (
        <div className="">
            
        </div>
    )
}